import { type PageRequest } from "@klyra/proto/src/codegen/cosmos/base/query/v1beta1/pagination";
import Long from "long";
// API options
export const DEFAULT_API_TIMEOUT = 5000;

export const SHORT_BLOCK_WINDOW = 20;
export const SHORT_BLOCK_FORWARD = 3;

// Broadcast options
export const DEFAULT_BROADCAST_OPTIONS = {
  pollIntervalMs: 300,
  timeoutMs: 8_000,
};

// Validation options
export const DEFAULT_SEQUENCE = 0;

// Querying
export const PAGE_REQUEST: PageRequest = {
  key: new Uint8Array(),
  offset: Long.UZERO,
  limit: Long.MAX_UNSIGNED_VALUE,
  countTotal: true,
  reverse: false,
};

// Gas configs
export const TDAI_DENOM = "utdai";
export const TDAI_DECIMALS = 6;
// TODO: check what this is
export const GAS_MULTIPLIER = 1.4;

export const MAX_UINT_32 = 4_294_967_295;

export { default as Messages } from "./messages";

export const BECH32_PREFIX = "klyra";
