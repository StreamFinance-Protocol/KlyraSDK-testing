import { LocalWallet } from "../lib/local-wallet";
type Wallet = LocalWallet;
type PrivateKey = Uint8Array | null;
type PublicKey = Uint8Array | null;
type Mnemonic = string;
export declare function getAccountFromUUID(uuid: string): Promise<{
    wallet: Wallet;
    mnemonic: Mnemonic;
    privateKey: PrivateKey;
    publicKey: PublicKey;
}>;
export declare function getAccountFromCredentials(username: string, pwd: string): Promise<{
    wallet: Wallet;
    mnemonic: Mnemonic;
    privateKey: PrivateKey;
    publicKey: PublicKey;
}>;
export declare function getAccountFromSignedMessage(signedMessage: string): Promise<{
    wallet: Wallet;
    mnemonic: Mnemonic;
    privateKey: PrivateKey;
    publicKey: PublicKey;
}>;
export {};
