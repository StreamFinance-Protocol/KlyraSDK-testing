import { type WebSocketMessage } from "@klyra/shared";

// TODO: see if this should be a class
// TODO: see if we can make this more generic
export interface BaseProcessor {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any -- we want to be able to handle any type
  processSubscribed: (msg: WebSocketMessage<any>) => void;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any -- we want to be able to handle any type
  processBatchUpdate: (msg: WebSocketMessage<any>) => void;
}
