import { type WebSocketMessage } from "@klyra/shared";
import { type LocalWallet } from "@klyra/chain";
import { type StateManagerConfig } from "../types/core";
import { type FormType } from "../types/forms";
import { type StoreState, type StateChangeListener } from "../types/store";
export declare class StateManager {
    private messageProcessor;
    private formProcessorFactory;
    private store;
    constructor(config: StateManagerConfig);
    setCurrentMarketTicker(marketId: string): void;
    handleMessage: (message: WebSocketMessage<unknown>) => void;
    subscribeToState(listener: StateChangeListener): () => void;
    getState(): StoreState;
    formInput(formType: FormType, fieldName: string, value: unknown): void;
    setAddress(address: string): void;
    setSubaccountNumber(subaccountNumber: number): void;
    setWalletInfo(localWallet: LocalWallet): void;
}
