import { type Store } from "../core/store";
import { type MarketOrderbook as OrderbookType } from "../types";
export declare function getOrderbookFromStore(store: Store, marketId: string): OrderbookType;
