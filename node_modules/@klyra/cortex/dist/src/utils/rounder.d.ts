export declare enum RoundingMode {
    TOWARDS_ZERO = 0,
    NEAREST = 1
}
export declare function quickRound(number: number, stepSize: number): number;
export declare function numberOfDecimals(stepSize: number): number;
export declare function round(number: number, stepSize: number, roundingMode?: RoundingMode): number;
export declare function roundDecimal(number: number, stepSize: number, roundingMode?: RoundingMode): number;
