import { type WebSocketMessage } from "@klyra/shared";
import { type Store } from "../core/store";
export declare class MessageProcessor {
    private accountProcessor;
    private marketsProcessor;
    private tradesProcessor;
    private orderbooksProcessor;
    private candlesProcessor;
    constructor(store: Store);
    process(message: WebSocketMessage<unknown>): void;
    private processMarketUpdate;
    private processTradeUpdate;
    private processOrderbookUpdate;
    private processAccountUpdate;
    private processCandleUpdate;
}
