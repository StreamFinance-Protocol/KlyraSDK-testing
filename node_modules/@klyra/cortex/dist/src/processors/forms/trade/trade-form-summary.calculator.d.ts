import { type Store } from "../../../core/store";
import { type TradeFormSummary, type TradeFormInputs, type Subaccount } from "../../../types";
export declare class TradeFormSummaryCalculator {
    private store;
    constructor(store: Store);
    calculate(calculationResult: TradeFormInputs, subaccount: Subaccount): TradeFormSummary | null;
    private orderbookMidPrice;
    private slippage;
    private getPositionMargin;
    private getPositionLeverage;
    private getExecutionPrice;
}
