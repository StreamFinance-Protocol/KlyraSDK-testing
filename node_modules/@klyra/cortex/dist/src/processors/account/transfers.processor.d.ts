import { type IndexerSubaccountTransfer } from "@klyra/shared";
import { type Store } from "../../core/store";
import { type SubaccountTransfer } from "../../types/accounts";
export declare class TransfersProcessor {
    private store;
    constructor(store: Store);
    process(existing: SubaccountTransfer[], payload: IndexerSubaccountTransfer[] | undefined): SubaccountTransfer[];
    private processTransfer;
}
