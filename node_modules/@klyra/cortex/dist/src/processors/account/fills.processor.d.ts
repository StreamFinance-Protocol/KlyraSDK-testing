import { type IndexerSubaccountFill } from "@klyra/shared";
import { type Store } from "../../core/store";
import { type SubaccountFill } from "../../types/accounts";
export declare class FillsProcessor {
    private store;
    constructor(store: Store);
    process(existing: SubaccountFill[], payload: IndexerSubaccountFill[] | undefined, subaccountNumber: number): SubaccountFill[];
    private processFill;
}
