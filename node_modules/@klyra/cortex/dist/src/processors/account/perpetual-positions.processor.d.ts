import { type IndexerSubaccountPerpetualPositionBatch, type IndexerSubaccountPerpetualPositionSubscribed } from "@klyra/shared";
import { type Store } from "../../core/store";
import { type SubaccountPosition } from "../../types/accounts";
export declare class PerpetualPositionsProcessor {
    private store;
    constructor(store: Store);
    process(existing: Record<string, SubaccountPosition>, payload: Record<string, IndexerSubaccountPerpetualPositionSubscribed> | undefined): Record<string, SubaccountPosition>;
    processChanges(existing: Record<string, SubaccountPosition>, payload: IndexerSubaccountPerpetualPositionBatch[] | undefined): Record<string, SubaccountPosition>;
    private processPerpetualPosition;
    private convertPerpetualPosition;
}
