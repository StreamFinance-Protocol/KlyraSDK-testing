import { type IndexerSubaccountOrder } from "@klyra/shared";
import { type Store } from "../../core/store";
import { type SubaccountOrder } from "../../types/accounts";
export declare class OrdersProcessor {
    private store;
    constructor(store: Store);
    process(subaccountOrders: SubaccountOrder[], payload: IndexerSubaccountOrder[] | undefined, subaccountNumber: number): SubaccountOrder[];
    private processOrder;
    private shouldUpdate;
}
