import { type IndexerSubaccountBatchUpdateMessage, type IndexerSubaccountSubscribedMessage } from "@klyra/shared";
import { type Store } from "../../core/store";
import { type BaseProcessor } from "../base.processor";
export declare class AccountProcessor implements BaseProcessor {
    private store;
    private subaccountProcessor;
    private accountBalancesProcessor;
    constructor(store: Store);
    processSubscribed(msg: IndexerSubaccountSubscribedMessage): void;
    processBatchUpdate(msg: IndexerSubaccountBatchUpdateMessage): void;
    private updateSubaccount;
    private updateSubaccounts;
}
