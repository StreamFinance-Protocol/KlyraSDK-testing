import { type IndexerCandlesBatchUpdateMessage, type IndexerCandlesSubscribedMessage } from "@klyra/shared";
import { type Store } from "../core/store";
import { type BaseProcessor } from "./base.processor";
export declare class CandlesProcessor implements BaseProcessor {
    private store;
    constructor(store: Store);
    processSubscribed(msg: IndexerCandlesSubscribedMessage): void;
    processBatchUpdate(msg: IndexerCandlesBatchUpdateMessage): void;
    private getMarketAndResolution;
    private processCandle;
    private updateCandles;
}
