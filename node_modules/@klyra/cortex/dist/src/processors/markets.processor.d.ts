import { type IndexerMarketSubscribedMessage, type IndexerMarketBatchUpdateMessage } from "@klyra/shared";
import { type Store } from "../core/store";
export declare class MarketsProcessor {
    private store;
    constructor(store: Store);
    processSubscribed(msg: IndexerMarketSubscribedMessage): void;
    processBatchUpdate(msg: IndexerMarketBatchUpdateMessage): void;
    private processSubscribedMarket;
    private processBatchUpdateMarketOraclePrices;
    private processBatchUpdateMarketTrading;
    private calculateEffectiveInitialMarginFraction;
    private calculatePerpetualMarketInfo;
    private calculateMarketConfigs;
    private calculateMarketStatus;
    private updateMarkets;
    private calculateMarket;
}
