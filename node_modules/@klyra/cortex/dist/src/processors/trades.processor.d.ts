import { type IndexerTradeBatchUpdateMessage, type IndexerTradeSubscribedMessage } from "@klyra/shared";
import { type Store } from "../core/store";
import { type BaseProcessor } from "./base.processor";
export declare class TradesProcessor implements BaseProcessor {
    private readonly TRADES_LIMIT;
    private store;
    constructor(store: Store);
    processSubscribed(msg: IndexerTradeSubscribedMessage): void;
    processBatchUpdate(msg: IndexerTradeBatchUpdateMessage): void;
    private processTrade;
    private updateMarketTrades;
}
