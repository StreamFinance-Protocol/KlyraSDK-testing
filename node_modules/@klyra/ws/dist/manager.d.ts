import { MessageChannel, WebSocketConfig, WebSocketMessage } from "@klyra/shared";
import { EventEmitter, EventCallback } from "@klyra/shared";
export declare class WebSocketManager {
    private ws;
    private url;
    private reconnectInterval;
    private intentionalClose;
    isConnected: boolean;
    eventEmitter: EventEmitter;
    subscribedChannels: [MessageChannel, string?][];
    constructor(config: WebSocketConfig);
    private getWebSocket;
    connect(): void;
    send(message: WebSocketMessage<unknown>): void;
    on(event: string, callback: EventCallback): void;
    off(event: string, callback: EventCallback): void;
    disconnect(): void;
}
